<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light  shadow">

    <a class="navbar-brand" href="{{ path('index') }}">HOME</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navToggledContent" aria-controls="navToggledContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navToggledContent">
        {% block navbody %}


        <ul class="navbar-nav mr-auto">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a class="nav-link" href="{{ path('logout') }}">Logout</a>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('login') }}">Login</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('register') }}">Register</a>
                </li>
            {% endif %}

        </ul>
        {% endblock %}
    </div>

</nav>